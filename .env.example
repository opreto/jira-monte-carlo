# Jira API Configuration
JIRA_URL=https://your-domain.atlassian.net
JIRA_USERNAME=your-email@example.com
ATLASSIAN_API_TOKEN=your-api-token-here

# Optional: Default project key for filtering
JIRA_PROJECT_KEY=

# Optional: JQL filter for issues (legacy - use FORECAST_JQL instead)
JIRA_JQL_FILTER=

# Dual JQL Support for separate velocity calculation and forecasting
# FORECAST_JQL: Filter for backlog items to be forecasted (incomplete work)
# Example: statusCategory != Done AND labels = mvp
FORECAST_JQL=

# HISTORY_JQL: Filter for completed items to calculate historical velocity
# Example: project = PROJ AND statusCategory = Done AND resolved >= -52w
HISTORY_JQL=